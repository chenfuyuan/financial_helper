# Application
APP_NAME=financial_helper
APP_ENV=development
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000

# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/financial_helper

# TuShare（股票数据，同步接口需要；无 token 时调用 /sync 会报错）
# 申请：https://tushare.pro/register
TUSHARE_TOKEN=

# CORS
CORS_ORIGINS=["http://localhost:3000"]

# Logging
LOG_LEVEL=DEBUG
