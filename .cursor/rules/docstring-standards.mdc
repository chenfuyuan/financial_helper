---
description: 注释与文档字符串规范 — 编辑任何 Python 文件时自动加载
globs: "**/*.py"
alwaysApply: false
---

# 注释与文档字符串规范

## 公开 API

模块与对外暴露的类、方法须有文档说明（中文），简述职责、行为与关键约束。

## 领域模型与 ORM 模型（必须）

类 docstring 中**必须**包含 **Attributes** 段，逐字段说明含义：

```python
"""简短类说明。

Attributes:
    id: 主键；新建未持久化时为 None。
    source: 数据来源（如 Tushare）。
    ...
"""
```

适用范围：`domain/entities/` 实体/聚合根 + `infrastructure/models/` ORM 模型。
新增或修改模型时须同步维护 Attributes 段。

## 行内注释

- 只解释「为什么」、业务/技术约束或非显然假设
- 不复述代码在做什么

## 禁止

- 无信息量注释、过期注释
- 复杂逻辑优先用命名与拆函数表达，不依赖长段注释
